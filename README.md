# Driftless — Agent Skill

Install with:

```
npx skills add joseluistello/driftless-skill
```

Driftless is the shared codebase memory layer for AI engineering teams. This skill teaches AI agents how to use the Driftless CLI for loading context, scanning diffs, and keeping code aligned with team rules.
